services:
  - type: web
    name: access-pro
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.6
      - key: PORT
        value: "8080"
